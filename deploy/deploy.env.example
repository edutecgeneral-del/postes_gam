# Copia este archivo a deploy/deploy.env y ajusta los valores.
# deploy.env está en .gitignore; no subas las credenciales.

SSH_HOST=123.45.67.89
SSH_USER=root
SSH_PORT=22
REMOTE_PATH=/var/www/field-coord
