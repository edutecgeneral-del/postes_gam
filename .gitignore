node_modules/
dist/
.DS_Store
*.zip
deploy/deploy.env
*:Zone.Identifier

# Patch scripts (uso único, no van al repo)
fix_*.py
paso*.py
patch_*.py
integra_*.py
geo.py
EOF
memory/
docs/
.idea/
.claude/
CLAUDE_CONTEXT.md
# Respaldo de .env (credenciales) y artefactos de build
.env.bak
*.bak
dist.tar.gz
apply_*.py

# Variables de entorno por ambiente (credenciales reales, NO subir a GitHub)
.env
.env.local
.env.v1
.env.v2
.env.v3
# Las plantillas .example SI se suben (no tienen credenciales reales)
!.env.*.example